export const config = {
  // sufficiently large max size as to allow all files through in default config
  FG_MAX_SIZE: '50mb',
  FG_MAX_SIZE_IGNORE_GLOB: false,
  FG_BLOCK_GLOBBED: false,
  FG_EXT_GLOB: '*',
};
