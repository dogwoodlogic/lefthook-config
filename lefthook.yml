# git hooks config for this repository
commit-msg:
  follow: true

pre-commit:
  follow: true

extends:
  - hooks/commit-msg/lint.yml
  - hooks/pre-commit/file-guard.yml
  - hooks/pre-commit/gitleaks.yml
  # - hooks/pre-commit/semgrep.yml
